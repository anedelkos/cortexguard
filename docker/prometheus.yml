global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/cortexguard_alerts.yml

scrape_configs:
  - job_name: "cortexguard_edge"
    static_configs:
      - targets: ["edge:8080"]
  - job_name: "cortexguard_cloud"
    static_configs:
      - targets: ["cloud-api:8001"]
